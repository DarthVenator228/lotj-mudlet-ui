[
  {
    "name": "system-map-radar",
    "patterns": [
      {
        "pattern": "^(?:\\b(?<class>[-\\w\\d ']+)\\b(?: '(?<shipName>[-\\w\\d ]+)')|\\(STAR\\) (?<starName>[-\\w\\d ]+)\\b|(?<planetName>[-\\w\\d ']+)\\b|(?<fin>Your Coordinates):) +(?<x>[-\\d]+) (?<y>[-\\d]+) (?<z>[-\\d]+)$",
        "type": "regex"
      }
    ]
  },
  {
    "name": "auto-radar",
    "patterns": [
      {
        "pattern": "The ship leaves the platform far behind as it flies into space.",
        "type": "exactMatch"
      },
      {
        "pattern": "Hyperjump complete.",
        "type": "exactMatch"
      },
      {
        "pattern": "The ship leaves the platform far behind as it completes its inaugural launch.",
        "type": "exactMatch"
      }
    ]
  },
  {
    "name": "hyperspace-calculation",
    "patterns": [
      {
        "pattern": "^\\[Status\\]: (Hyperspace) calculations have been completed\\.$",
        "type": "regex"
      }
    ]
  }
]